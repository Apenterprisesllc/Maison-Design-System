/**
 * Portal catalogue used to live here as a hard-coded array.
 * Catalogue now comes from Supabase via `lib/api/services.ts`; this file
 * is intentionally empty and kept only as a placeholder so existing imports
 * don't break during migration. Delete once no module imports from it.
 */
export {};
